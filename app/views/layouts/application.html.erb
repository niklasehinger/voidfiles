<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Voidfiles" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/favicon.png" type="image/png">
    <link rel="icon" href="/voidfiles_64x64.png" type="image/png">
    <link rel="apple-touch-icon" href="/voidfiles_64x64.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="bg-gradient-to-br from-voidfiles-white to-gray-50 min-h-screen flex flex-col">
    <%= render 'shared/header' %>

    <main class="flex-1">
      <% if notice %>
        <div class="max-w-6xl mx-auto px-6 pt-6">
          <div class="bg-green-50 border border-green-200 text-green-700 px-4 py-3 rounded-lg mb-4" role="alert">
            <span class="block sm:inline"><%= notice %></span>
          </div>
        </div>
      <% end %>
      <% if alert %>
        <div class="max-w-6xl mx-auto px-6 pt-6">
          <div class="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded-lg mb-4" role="alert">
            <span class="block sm:inline"><%= alert %></span>
          </div>
        </div>
      <% end %>
      <%= yield %>
    </main>

    <%= render 'shared/footer' %>
  </body>
</html>
